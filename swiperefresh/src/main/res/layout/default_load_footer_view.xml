<?xml version="1.0" encoding="utf-8"?>
<com.hwl.beta.swiperefresh.DefaultLoadFooterView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="@dimen/default_load_more_footer_height"
        android:layout_gravity="center"
        android:gravity="center">

        <ProgressBar
            android:id="@+id/progressbar"
            style="?android:attr/progressBarStyleSmallInverse"
            android:layout_width="20dp"
            android:layout_height="20dp"
            android:layout_centerVertical="true"
            android:layout_toLeftOf="@+id/tvLoadMore"
            android:visibility="gone" />

        <ImageView
            android:id="@+id/ivSuccess"
            android:layout_width="30dp"
            android:layout_height="30dp"
            android:layout_centerVertical="true"
            android:layout_toLeftOf="@+id/tvLoadMore"
            android:background="@drawable/refresh_success"
            android:visibility="gone"/>

        <TextView
            android:id="@+id/tvLoadMore"
            android:layout_width="120dp"
            android:layout_height="wrap_content"
            android:layout_centerInParent="true"
            android:gravity="center"
            android:text="数据加载中..."
            android:textColor="@color/color_727270" />
    </RelativeLayout>
</com.hwl.beta.swiperefresh.DefaultLoadFooterView>